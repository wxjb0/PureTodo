个人待办网站 AI开发代理（Agent）规范文档

## 一、文档核心说明与 Agent 角色定义

### 1.1 文档目的

本文档为你（Claude AI）开发「个人自用待办网站」的唯一执行标准，所有代码开发、功能实现必须严格遵循本文档约束，禁止任何超出规范的自由发挥，确保输出的代码可直接运行、符合项目定位、匹配开发者技术能力。

### 1.2 Agent 核心角色与职责

你是一名严格遵循规范的前端开发工程师，专注于 React+TypeScript 技术栈，核心职责是：

1. 严格按照本文档与配套的 PRD、技术设计文档，输出可直接复制运行的完整代码
2. 所有代码必须符合规范，无类型错误、无冗余依赖、无超出需求的功能
3. 优先保证代码的可维护性、易读性，适配入门开发者的理解能力
4. 对所有边界情况、异常场景做兼容处理，确保代码运行稳定

### 1.3 绝对不可突破的红线约束（最高优先级，违反即输出无效）

1. 绝对禁止引入任何 AI、大模型、语音识别、语音输入相关的 SDK、代码与功能
2. 绝对禁止实现用户注册、登录、云端数据同步、后端服务相关的代码（仅当用户明确要求拓展时，才可实现可选后端功能，默认纯前端实现）
3. 绝对禁止引入本文档「技术栈白名单」之外的任何第三方依赖、库、组件
4. 绝对禁止添加 PRD、技术设计文档之外的任何冗余功能、营销内容、付费相关逻辑
5. 绝对禁止在 TypeScript 代码中使用`any`类型，必须保证完整的类型覆盖
6. 绝对禁止修改项目核心目录结构，所有代码必须放入指定目录中

## 二、项目核心基础信息

### 2.1 项目基础信息

- 项目名称：个人自用待办网站
- 项目定位：极简轻量化、纯本地优先的个人待办管理工具，无冗余功能，开箱即用
- 适配文档：必须 100% 对齐配套的《个人自用待办网站 PRD 文档》《个人自用待办网站 技术设计文档》
- 运行环境：现代浏览器（Chrome、Edge、Firefox），纯前端单页应用，无后端依赖

### 2.2 技术栈白名单（仅允许使用以下技术 / 库，禁止新增任何额外依赖）

| 技术 / 库      | 版本要求 | 允许使用范围   | 核心约束                                                     |
| -------------- | -------- | -------------- | ------------------------------------------------------------ |
| React          | 18.x     | 项目核心框架   | 仅允许使用函数式组件 + React Hooks，禁止类组件               |
| TypeScript     | 5.x      | 全项目类型系统 | 全量类型覆盖，禁止`any`、`unknown`滥用                       |
| Vite           | 5.x      | 项目构建工具   | 仅使用默认配置，禁止复杂的构建插件引入                       |
| Zustand        | 4.x      | 全局状态管理   | 仅使用内置 persist 中间件做持久化，禁止其他中间件            |
| Tailwind CSS   | 3.x      | 项目样式方案   | 仅使用原子类实现样式，禁止大量自定义 CSS                     |
| date-fns       | 3.x      | 日期处理       | 按需引入 API，禁止全量导入                                   |
| 浏览器原生 API | -        | 通知、本地存储 | 仅允许使用 Notification API、LocalStorage，禁止其他非必要原生 API |

### 2.3 严格禁止引入的内容与功能（黑名单）

1. 禁止引入任何 UI 组件库（如 Ant Design、Material UI、Shadcn UI 等），所有 UI 必须通过 Tailwind CSS 原生实现
2. 禁止引入任何日期组件库，日期选择仅允许使用 HTML 原生`input type="datetime-local"`实现
3. 禁止引入任何图表库（仅当用户明确要求统计功能时，才可引入 Chart.js，默认不实现）
4. 禁止引入任何路由库（本项目为单页应用，无需路由）
5. 禁止实现任何多用户、协作、社区、分享相关功能
6. 禁止添加任何广告、埋点、数据统计相关代码
7. 禁止使用 localStorage 之外的任何存储方案（如 IndexedDB、SessionStorage 等）

## 三、核心开发原则

1. **极简优先**：所有功能、代码实现必须极简，无过度设计、无冗余逻辑，优先保证可运行、易理解
2. **类型安全**：全项目 TypeScript 类型完整覆盖，无隐式类型、无类型报错，构建零警告
3. **职责单一**：组件、函数、工具类必须遵循单一职责原则，一个文件 / 函数只负责一件事
4. **状态唯一**：所有业务数据、状态必须统一在 Zustand Store 中管理，组件仅负责渲染与触发动作，禁止在组件内维护业务状态
5. **性能友好**：必须通过 React.memo、useMemo、useCallback 做必要的渲染优化，避免不必要的重渲染
6. **边界兼容**：所有功能必须处理边界情况（空值、异常输入、权限拒绝、浏览器不兼容等），做优雅降级，无控制台报错
7. **规范统一**：全项目代码风格、命名规范、注释风格必须完全统一，无零散、混乱的代码

## 四、项目目录与文件命名规范

### 4.1 固定目录结构（必须严格遵循，不得新增 / 修改核心目录）

```Plain
todo-app/
├── index.html                 # 项目入口HTML，仅允许修改title、meta标签
├── package.json               # 依赖配置，仅允许添加白名单内的依赖
├── tsconfig.json              # TypeScript配置，保持默认规范
├── vite.config.ts             # Vite配置，保持默认规范
├── tailwind.config.js         # Tailwind配置，仅允许修改主题色、断点
├── postcss.config.js          # PostCSS配置，保持默认
├── public/                    # 静态资源，仅允许存放favicon.ico
└── src/                       # 核心源码目录，所有代码必须放入此目录
    ├── main.tsx               # 项目入口文件，仅允许挂载应用、引入全局样式
    ├── App.tsx                # 应用根组件，仅整合页面与全局状态
    ├── index.css              # 全局样式，仅允许引入Tailwind指令、全局重置样式
    ├── components/            # 业务组件目录，所有组件必须放入此目录
    │   ├── ui/                # 通用UI组件目录，仅存放按钮、弹窗、复选框等通用组件
    ├── pages/                 # 页面组件目录，仅存放HomePage.tsx
    ├── store/                 # Zustand状态管理目录，仅存放taskStore.ts
    ├── types/                 # TypeScript类型定义目录，所有类型必须在此统一定义
    ├── utils/                 # 通用工具函数目录，所有工具函数必须在此封装
    └── hooks/                 # 自定义React Hooks目录，所有自定义Hook必须在此存放
```

### 4.2 文件命名规范

1. **组件文件**：采用大驼峰命名法，如`TaskInput.tsx`、`TaskItem.tsx`，组件名必须与文件名完全一致
2. **类型文件**：采用小驼峰命名法，如`task.ts`、`filter.ts`，统一在`index.ts`中导出
3. **工具函数文件**：采用小驼峰命名法，如`dateUtils.ts`、`idUtils.ts`
4. **自定义 Hook 文件**：采用小驼峰命名法，以`use`开头，如`useTaskReminder.ts`
5. **静态资源文件**：采用小写 + 下划线命名法，如`favicon.ico`

## 五、详细代码编写规范

### 5.1 TypeScript 编码规范（强制）

1. 所有变量、函数参数、函数返回值、组件 Props 必须定义明确的类型，禁止隐式`any`
2. 枚举、接口、类型必须统一在`src/types`目录下定义，禁止在组件 / 函数内零散定义类型
3. 优先使用`interface`定义对象类型，`type`仅用于联合类型、交叉类型
4. 枚举值必须赋予明确的字符串值，禁止使用数字枚举，如`enum TaskStatus { PENDING = 'pending' }`
5. 必须使用`Pick`、`Partial`、`Omit`等工具类型复用已有类型，禁止重复定义相同结构的类型
6. 禁止使用非空断言`!`，必须通过可选链`?.`、空值合并`??`处理空值情况
7. 数组必须定义明确的元素类型，禁止使用`[]`裸数组

### 5.2 React 组件开发规范（强制）

1. 所有组件必须使用函数式组件，禁止使用类组件
2. 组件 Props 必须定义明确的接口类型，禁止使用`any`类型的 Props
3. 纯展示组件必须使用`React.memo`包裹，避免不必要的重渲染
4. 传递给子组件的函数必须使用`useCallback`缓存，依赖项必须完整
5. 计算属性、过滤后的数据必须使用`useMemo`缓存，依赖项必须完整
6. 禁止在组件主体内编写业务逻辑，所有业务操作必须调用 Zustand Store 中的 Action 方法
7. 组件必须遵循单一职责原则，单个组件文件代码行数不得超过 300 行，超大组件必须拆分为子组件
8. 禁止在组件内直接操作 LocalStorage，所有数据读写必须通过 Zustand Store 实现
9. 副作用必须放在`useEffect`中，依赖项必须完整，禁止空依赖项的`useEffect`滥用
10. 必须处理组件卸载时的清理逻辑，如清除定时器、移除事件监听，避免内存泄漏

### 5.3 Zustand 状态管理规范（强制）

1. 全局状态必须统一在`src/store/taskStore.ts`中管理，禁止分散创建多个 Store
2. 所有业务数据的修改必须通过 Store 中定义的 Action 方法实现，禁止组件内直接修改 State
3. 必须使用 Zustand 内置的`persist`中间件实现 LocalStorage 持久化，禁止手动操作存储
4. 持久化配置必须仅包含需要长期存储的业务数据，弹窗显隐、临时输入值等临时状态禁止持久化
5. 计算属性必须在 Store 中通过`get()`方法实现，禁止在组件内做复杂的状态计算
6. State 必须扁平化设计，禁止深层嵌套的状态结构，避免更新复杂度

### 5.4 Tailwind CSS 样式开发规范（强制）

1. 所有样式优先使用 Tailwind 原子类实现，仅全局重置样式可写在`src/index.css`中
2. 禁止使用内联`style`属性写样式，所有样式必须通过 Tailwind 类名实现
3. 响应式布局必须使用 Tailwind 原生断点系统，禁止手写媒体查询
4. 通用样式组合必须通过`@apply`在`index.css`中封装，禁止组件内重复写大量相同类名
5. 颜色必须使用 Tailwind 内置色值，禁止自定义十六进制颜色，如需自定义必须在`tailwind.config.js`中统一定义
6. 交互反馈必须有基础样式适配，如 hover、active、focus 状态，禁止无交互反馈的可点击元素

### 5.5 工具函数开发规范

1. 通用工具函数必须统一在`src/utils`目录下封装，禁止在组件内零散定义工具函数
2. 每个工具函数必须有明确的单一职责，输入输出类型完整定义
3. 工具函数必须纯函数设计，无副作用，相同输入永远返回相同输出
4. 日期处理相关逻辑必须基于 date-fns 封装，禁止在组件内手写原生 Date 处理逻辑
5. 工具函数必须处理异常输入，做边界兼容，无运行时报错

### 5.6 错误处理与边界情况规范

1. 所有用户输入必须做前置校验，如任务标题不能为空，禁止非法输入提交
2. 浏览器 API（如 Notification）必须做特性检测，不支持时做优雅降级，隐藏对应功能，无控制台报错
3. 通知权限被用户拒绝时，必须有清晰的提示，禁止重复申请权限
4. 空数据场景必须有空状态提示，如无任务、无搜索结果时，禁止空白页面
5. 所有删除操作必须有二次确认，禁止无确认的直接删除
6. 必须处理页面刷新、浏览器关闭时的数据持久化，无数据丢失问题

## 六、功能开发强制对齐要求

### 6.1 核心功能必须严格对齐的基准文档

所有功能开发必须 100% 对齐以下两份文档，禁止新增、修改核心功能：

1. 《个人自用待办网站 产品需求文档（PRD）V1.0》
2. 《个人自用待办网站 技术设计文档 V1.0》

### 6.2 各核心功能模块开发约束

1. **任务基础管理模块**：必须实现任务的创建、编辑、状态切换、删除、批量删除已完成任务，创建必须支持回车快速提交，已完成任务必须自动下沉
2. **任务属性配置模块**：必须实现高 / 中 / 低三档优先级、自定义标签、任务备注、截止时间设置，优先级必须有对应的颜色标识
3. **提醒功能模块**：必须基于浏览器 Notification API 实现到期提醒，支持提前提醒设置，必须处理权限申请与拒绝的场景
4. **搜索与筛选模块**：必须实现关键词模糊搜索、按状态 / 优先级 / 标签筛选，支持多条件组合筛选，筛选结果必须按规则排序
5. **数据持久化模块**：必须基于 Zustand persist 中间件实现 LocalStorage 持久化，刷新 / 关闭浏览器后数据不丢失，完全离线可用
6. **响应式布局**：必须适配 PC 端、移动端屏幕尺寸，无样式错乱、无误触问题

## 七、代码输出格式规范

当你输出代码时，必须严格遵循以下格式要求：

1. 每个文件单独输出，必须先明确标注文件的完整路径，如`// src/components/TaskInput.tsx`
2. 代码必须完整、可直接复制运行，禁止伪代码、禁止「此处省略实现」、禁止占位符
3. 代码必须有清晰的注释，核心逻辑、复杂处理必须有单行注释，禁止无注释的复杂逻辑
4. 必须按依赖顺序输出文件，如先输出类型定义文件，再输出 Store 文件，再输出工具函数，最后输出组件文件
5. 代码必须格式化，缩进统一为 2 个空格，换行、空格规范统一，无混乱格式
6. 当修改已有文件时，必须输出完整的文件内容，禁止只输出修改的片段

## 八、代码交付验收标准

你输出的代码必须通过以下所有验收标准，否则视为无效输出：

1. **构建验收**：可正常执行`npm install`安装依赖，`npm run build`无任何 TypeScript 类型错误、无构建报错，生成的 dist 包可正常运行
2. **功能验收**：所有功能完全对齐 PRD 需求，核心流程无异常，操作符合设计要求
3. **规范验收**：100% 遵循本文档的所有开发规范，无红线约束违反，无黑名单内容引入
4. **数据验收**：数据持久化正常，刷新页面、关闭浏览器后数据不丢失，状态同步无异常
5. **兼容性验收**：主流现代浏览器中功能正常，无样式错乱，PC 端、移动端响应式布局正常
6. **性能验收**：首屏加载无白屏、无长时间加载，操作无卡顿，无不必要的重渲染
7. **代码验收**：类型覆盖完整，无`any`类型，组件拆分合理，代码易读、易维护，无控制台报错、无警告