1. ## 一、文档基础信息

   - 项目名称：轻办（无复杂功能，简洁可用）
   - 文档版本：V1.0（基础可用版）
   - 更新日期：2026年05月
   - 产品定位：个人自用、极简轻量化、纯本地存储的待办管理工具，无需复杂功能，满足日常任务记录与管理需求，适配个人开发练手（React+TypeScript+Vite技术栈）
   - 技术栈约束：前端 React+TypeScript+Vite、状态管理 Zustand、样式 Tailwind CSS、日期处理 date-fns、数据存储 LocalStorage（暂不使用MySQL，后续可按需拓展）
   - 核心边界：无AI、无语音识别、无云端服务、无冗余功能，仅满足个人日常自用，快速开发落地

   ## 二、文档概述

   ### 2.1 文档目的

   明确个人自用待办网站的核心功能、交互规则、技术规范，为个人开发提供清晰、可落地的执行依据，优先实现基础可用功能，降低开发难度，确保快速落地并满足个人日常使用需求。

   ### 2.2 产品定位

   极简、轻便、本地优先的个人待办管理工具，无需注册登录，打开即可使用，专注解决个人“记录待办、标记完成、避免遗漏”的核心需求，无任何冗余功能，同时作为React技术栈练手项目，便于后续按需优化。页面要求足够简单优雅。

   ### 2.3 阅读对象

   仅本人（项目开发者），用于明确开发需求、梳理开发流程、验收功能效果。

   ## 三、核心用户与使用场景

   ### 3.1 核心用户

   本人（开发者），用于日常记录工作、学习、生活中的待办事项，需要简单、高效、无干扰的管理工具，同时通过开发过程熟悉React相关技术栈。

   ### 3.2 核心使用场景

   - 快速记录：打开网站就能添加待办任务，无需复杂操作，避免忘记重要事项
   - 状态管理：标记任务完成/未完成，清晰区分待办与已办，直观查看任务进度
   - 分类筛选：按优先级、标签分类管理任务，快速找到目标任务，提升使用效率
   - 到期提醒：给重要任务设置截止时间，通过浏览器通知提醒，避免错过deadline
   - 离线使用：无需联网，数据存储在本地，随时打开均可查看、编辑任务

   ## 四、核心功能需求详情（V1.0 基础可用版，必做）

   所有功能均围绕“个人自用、简单可用”设计，开发难度低，优先落地，后续不强制迭代，按需优化。

   ### 4.1 任务基础管理功能

   核心功能，实现待办任务从创建到删除的全流程管理，操作简单无冗余。

   - 任务创建：首页顶部固定输入框，输入任务标题即可快速创建，支持回车确认；创建后自动生成唯一ID和创建时间，默认状态为“未完成”，默认优先级为“中”；无需跳转页面，一键完成创建。
   - 任务编辑：点击任务条目可打开编辑弹窗，可修改任务标题、备注、截止时间、优先级、标签；编辑完成点击保存生效，取消则不修改原有数据，操作直观。
   - 状态切换：任务条目左侧设置复选框，勾选即标记为“完成”，取消勾选则恢复“未完成”；已完成任务自动下沉到列表底部，添加文字划线、置灰样式，与未完成任务区分；状态切换实时同步本地存储，无延迟。
   - 任务删除：单条任务删除入口放在编辑弹窗内，点击删除需二次确认，避免误删；支持一键批量删除所有已完成任务，删除后数据从LocalStorage中永久移除，无法恢复。

   ### 4.2 任务属性配置功能

   辅助管理任务，便于分类和区分重要程度，操作简单，无需复杂配置。

   - 优先级标记：支持设置高、中、低三个优先级，对应不同颜色（高优红色、中优黄色、低优灰色）；列表支持按优先级排序，高优任务默认置顶，优先级可随时修改，实时更新样式和排序。
   - 自定义标签：支持创建自定义标签（如“工作”“学习”“生活”），标签名称可自定义，数量无上限；单个任务可绑定多个标签，支持删除无用标签，删除后已绑定该标签的任务自动解绑；标签以圆角色块展示在任务条目上，便于区分。
   - 任务备注：支持为任务添加多行备注，补充任务细节；备注为选填项，随任务同步存储，编辑任务时可随时修改；列表条目可预览备注前1行内容，点击可展开查看完整备注。

   ### 4.3 截止时间与提醒功能

   避免遗漏重要任务，无需复杂提醒逻辑，基于浏览器原生能力实现。

   - 截止时间设置：创建/编辑任务时，可通过日期选择器设置截止日期和具体时间；不设置则无提醒，已过期的未完成任务，在列表中添加红色过期标识，便于关注。
   - 本地定时提醒：基于浏览器Notification API实现到期提醒，提醒时间与截止时间一致，可选择提前5/10/30分钟提醒；首次使用需申请通知权限，用户拒绝后不再重复弹窗；仅在浏览器打开状态下触发提醒，点击通知可快速跳转到网站并定位对应任务。

   ### 4.4 搜索与筛选功能

   快速定位目标任务，提升使用效率，逻辑简单，无需复杂查询。

   - 任务搜索：页面顶部设置搜索框，输入关键词可模糊搜索任务标题、备注内容，搜索结果实时刷新；无匹配结果时，展示“暂无匹配任务”提示，无输入时展示全部任务。
   - 任务筛选：搜索框下方设置筛选按钮，支持按优先级（高/中/低）、标签、完成状态（全部/未完成/已完成）筛选；可单条件筛选，也可多条件组合筛选，选中的筛选条件有高亮标识，支持一键清空筛选。

   ### 4.5 数据持久化功能

   保障数据不丢失，无需手动保存，完全离线可用。

   - 本地存储：所有任务数据、标签数据均存储在浏览器LocalStorage中，不上传任何云端，无隐私泄露风险；基于Zustand持久化中间件，实现状态变化实时同步到LocalStorage，无需手动保存。
   - 离线可用：刷新页面、关闭浏览器后重新打开，所有数据不丢失，无需联网即可正常使用所有功能；无强制注册、登录步骤，打开网站即可直接操作。

   ### 4.6 界面与交互功能

   极简界面，操作简单，适配个人常用设备，无学习成本。

   - 响应式布局：基于Tailwind CSS实现，适配PC端、手机端浏览器，无横向滚动条，核心操作按钮尺寸适配移动端点击，无误触问题。
   - 极简交互：核心操作路径不超过2步，无复杂动效，界面无冗余元素，聚焦任务列表；操作有轻微视觉反馈（如状态切换、按钮点击），提升使用体验。

   ## 五、非功能需求（贴合个人开发，降低难度）

   - 性能需求：基于Vite构建，首屏加载时间≤2s，页面切换无卡顿，任务增删改查操作实时响应，无明显延迟。
   - 兼容性需求：兼容Chrome、Edge、Firefox等主流现代浏览器，PC端、手机端打开无样式错乱，功能正常使用。
   - 易用性需求：操作逻辑简单，无需查看教程即可上手；异常操作（如删除、输入为空）有清晰提示，避免操作困惑。
   - 可维护性需求：组件化拆分代码，使用TypeScript提供类型约束，Zustand状态管理模块化，便于后续按需修改或新增功能。

   ## 六、技术栈规范（严格匹配个人开发需求）

   - 前端核心框架：React 18 + TypeScript + Vite，实现项目基础搭建，TypeScript保障代码规范，Vite提升构建和热更新速度。
   - 状态管理：Zustand，轻量级状态管理库，管理全局任务数据、标签数据，搭配持久化中间件，实现LocalStorage数据同步。
   - 样式方案：Tailwind CSS，原子化CSS框架，快速实现响应式布局和页面样式，无需编写大量自定义CSS，提升开发效率。
   - 日期处理：date-fns，轻量级日期工具库，实现日期格式化、时间对比、截止时间计算等功能，简单易上手。
   - 数据存储：浏览器LocalStorage，纯前端本地存储，无后端依赖，实现数据持久化和离线可用，降低开发难度。

   ## 七、功能验收标准（个人自用，简单明确）

   - 核心流程：打开网站无需注册登录，可直接创建、编辑、删除任务，状态切换正常，数据实时同步，刷新/关闭浏览器后数据不丢失。
   - 功能可用：优先级、标签、截止时间、提醒、搜索筛选功能均能正常使用，无异常报错。
   - 兼容性：主流浏览器、PC端/手机端打开，界面无错乱，操作无异常。
   - 性能：首屏加载快，操作无卡顿，任务数量较多时（≥50条）仍能正常使用。